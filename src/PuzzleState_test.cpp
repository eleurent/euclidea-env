#include <gtest/gtest.h>
#include "PuzzleState.h"

TEST(PuzzleStateTest, DrawLineTest) {
    // Write test cases for drawLine function
    EXPECT_TRUE(true);
}

TEST(PuzzleStateTest, DrawCircleTest) {
    // Write test cases for drawCircle function
    EXPECT_TRUE(true);
}
