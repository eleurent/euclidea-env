#include "PuzzleState.h"

PuzzleState drawLine(const PuzzleState& currentState, const Point& start, const Point& end) {
    PuzzleState updatedState = currentState;
    return updatedState;
}

PuzzleState drawCircle(const PuzzleState& currentState, const Point& center, double radius) {
    PuzzleState updatedState = currentState;
    return updatedState;
}